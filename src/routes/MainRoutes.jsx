import { createBrowserRouter } from "react-router-dom";

const MainRoutes = createBrowserRouter([
    {
        path: '/',
        element: <div className="font-jSans">Hello world!!</div>
    }
])

export default MainRoutes;